import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bradskine.expensetracker',
  appName: 'Expense Tracker ',
  webDir: 'dist/app-v0.1/browser'
};

export default config;
